services:
  outage-tracker:
    build: .
    container_name: outage-tracker
    restart: unless-stopped
    ports:
      - "8585:8080"
    volumes:
      - /mnt/Apps/Storage/outage-tracker:/app/data
    environment:
      - TZ=America/New_York