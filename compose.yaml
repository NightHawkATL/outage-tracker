services:
  outage-tracker:
    build: .
    container_name: outage-tracker
    restart: unless-stopped
    ports:
      - "8585:8080"
    volumes:
      - /mnt/Apps/Storage/outage-tracker:/app/data  # This saves your UI settings permanently
    environment:
      - TZ=${TZ}